import {Link} from "react-router-dom";const v={primary:"bg-primary-500 text-white hover:bg-primary-600",outline:"border border-primary-500 bg-white text-primary-500 hover:bg-primary-50",dark:"bg-slate-900 text-white hover:bg-slate-800",accent:"bg-accent text-slate-950 hover:brightness-95"};export default function Button({children,to,href,variant="primary",className="",type="button",...props}){const c=`inline-flex items-center justify-center rounded-xl px-5 py-3 text-sm font-bold transition focus:outline-none focus:ring-2 focus:ring-primary-500 focus:ring-offset-2 ${v[variant]} ${className}`;if(to)return <Link to={to} className={c}>{children}</Link>;if(href)return <a href={href} className={c}>{children}</a>;return <button type={type} className={c} {...props}>{children}</button>}
